@model CarInsurance.Models.Insuree

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Insuree</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        @Html.HiddenFor(model => model.FirstName)

        @Html.HiddenFor(model => model.LastName)

        @Html.HiddenFor(model => model.EmailAddress)

        @Html.HiddenFor(model => model.DateOfBirth)

        @Html.HiddenFor(model => model.CarYear)

        @Html.HiddenFor(model => model.CarMake)

        @Html.HiddenFor(model => model.CarModel)

        @Html.HiddenFor(model => model.DUI)

        @Html.HiddenFor(model => model.SpeedingTickets)

        @Html.HiddenFor(model => model.CoverageType)

        @Html.HiddenFor(model => model.Quote)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
